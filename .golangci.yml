version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - errcheck
  settings:
    errcheck:
      check-blank: true
  exclusions:
    rules:
      - path: '.*_test\.go$'
        linters:
          - errcheck
