name: fuzz

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fuzz:
    name: ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { pkg: ./internal/utils/patch, target: FuzzParsePatch }
          - { pkg: ./internal/capabilities/plugins, target: FuzzExtractPluginZip }
          - { pkg: ./internal/capabilities/tools/assembly, target: FuzzRedactContent }
          - { pkg: ./internal/capabilities/skills, target: FuzzSkillFrontmatter }
          - { pkg: ./internal/orchestration/engine, target: FuzzExecPolicyRule }
          - { pkg: ./internal/capabilities/sessions, target: FuzzRequireID }
          - { pkg: ./internal/foundation/config, target: FuzzUserConfigYAML }
          - { pkg: ./internal/capabilities/execd, target: FuzzRPCRequest }
          - { pkg: ./internal/utils/extract, target: FuzzStripHiddenHTML }
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: Fuzz ${{ matrix.target }}
        run: go test -run '^$' -fuzz ${{ matrix.target }} -fuzztime 60s ${{ matrix.pkg }}
