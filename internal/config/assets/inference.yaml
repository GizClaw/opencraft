# opencraft user-editable DeepSeek provider settings
# (~/.opencraft/config/inference.yaml; deleted files are regenerated).
# This file is the settings of the provider.deepseek resource;
# ${env:...} references are expanded at build time.
id: deepseek
profiles:
  - secrets:
      api_key: ${env:DEEPSEEK_API_KEY}
