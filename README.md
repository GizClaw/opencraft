# opencraft

A Go coding agent built on flowcraft `sdk/sdkx`.

Status: framework scaffolding in progress. Pinned flowcraft `sdk v0.5.3`
(`sdkx v0.5.5` next); landed so far: SQLite state store, memory summary
(buffer fold), exec-server protocol types, CLI entrypoints.

Design documentation is maintained locally under `docs/` and is not committed
to this repository.
